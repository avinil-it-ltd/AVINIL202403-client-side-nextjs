import Login from '../../Auth/Login/Login';

export const metadata = {
  title: 'Login | 3p Communication',
  description: 'Sign in to 3P Communication dashboard.',
};

export default function LoginPage() {
  return <Login />;
}
