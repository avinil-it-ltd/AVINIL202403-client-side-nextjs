import Register from '../../Auth/Register/Register';

export const metadata = {
  title: 'Register | 3p Communication',
  description: 'Create an account on 3P Communication.',
};

export default function RegisterPage() {
  return <Register />;
}
